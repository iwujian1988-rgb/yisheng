/* pages/admin/activation-list.wxss */
.list-container { min-height: 100vh; background-color: #F5F7FA; padding: 16px 32px; }
.filter-bar { display: flex; gap: 10px; margin-bottom: 16px; }
.filter-item { padding: 8px 16px; border-radius: 20px; background-color: #FFFFFF; }
.filter-active { background-color: #4A90E2; }
.filter-active .filter-text { color: #FFFFFF; }
.filter-text { font-size: 14px; color: #666666; }
.empty-section { display: flex; justify-content: center; padding-top: 120px; }
.empty-text { font-size: 14px; color: #999999; }
.code-list { display: flex; flex-direction: column; gap: 10px; }
.code-card { background-color: #FFFFFF; border-radius: 8px; padding: 16px 20px; display: flex; justify-content: space-between; align-items: center; }
.code-text { font-size: 14px; color: #333333; font-family: monospace; }
.code-status { font-size: 13px; color: #999999; }
